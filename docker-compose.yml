version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEYS=dev-key-12345
      - STORAGE_PATH=/data
      - ENVIRONMENT=development
      - ESV_API_KEY=
    volumes:
      - ./app:/app/app:ro
      - latex_data:/data

volumes:
  latex_data:
