# Database (auto-set by Railway when you add PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/latexgen

# API Keys (comma-separated list for initial setup)
API_KEYS=your-secret-api-key-here

# Storage path for styles, fonts, and temp files
STORAGE_PATH=/data

# Optional: Set to "development" for debug mode
ENVIRONMENT=production
